<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/progress_background"
    style="@style/CSVerticalLayoutWrap"
    android:layout_gravity="center"
    android:background="?attr/colorSurface"
    android:padding="15dp">

    <TextView
        android:id="@+id/progress_title"
        style="@style/CSWrap"
        android:layout_gravity="center"
        android:gravity="center"
        android:text="Progress Title"
        android:textAppearance="@style/CSTextSubtitle1"
        android:textColor="?attr/colorPrimary" />

    <ProgressBar
        android:id="@+id/progress_indicator"
        style="@style/Widget.MaterialComponents.CircularProgressIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_margin="25dp"
        android:indeterminate="true"
        android:indeterminateTint="?attr/colorPrimary" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/progress_button_cancel"
        style="@style/CSTextButtonWrap"
        android:layout_gravity="center"
        android:text="@string/cs_dialog_cancel"
        android:textAppearance="@style/CSTextButton"
        android:textColor="?attr/colorPrimary" />

</LinearLayout>