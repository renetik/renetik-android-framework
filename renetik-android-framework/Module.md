# Module renetik-android-framework

Aggregate artifact that pulls in every runtime module as an `api` dependency —
core, event, json, store, preset, ui, ui-picker, material, imaging and
controller. It contains no code of its own; depend on it to get the whole
framework in one line, or pick individual modules for a smaller footprint.
